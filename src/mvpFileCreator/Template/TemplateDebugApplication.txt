package debug;

import com.alibaba.android.arouter.launcher.ARouter;
import com.credithc.common_library.BaseApplication;

public class $moduleNameApplication extends BaseApplication {

    @Override
    public void onCreate() {
        super.onCreate();
        ARouter.openDebug();
        ARouter.openLog();
        ARouter.init(this);
    }
}
